import { ControllerArgs, ReturnValue } from "../utils";

export class VidoeService {

    async removeAudio({}: ControllerArgs): Promise<ReturnValue> {
        return {}
    }

    async getMetadata({}: ControllerArgs): Promise<ReturnValue> {
        return {}
    }

    async addNewAudio({}: ControllerArgs): Promise<ReturnValue>{
        return {}
    }
}